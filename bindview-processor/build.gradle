apply plugin: 'java'

dependencies {
    compile fileTree(dir: 'libs', include: ['*.jar'])
    compile project(':bindview-api')
    compile 'com.squareup:javapoet:1.8.0'
}

sourceCompatibility = "1.8"
targetCompatibility = "1.8"
